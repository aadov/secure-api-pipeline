version: "3.9"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
